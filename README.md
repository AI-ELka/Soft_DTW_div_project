Differentiable Divergences between Time Series
==============================================

Run 
```bash
pip install -r requirements.txt
```

Then go to `tests/final_deliverable.ipynb`. The code in `sdtw_div` folder contains imported code implementing the algorithms taken from papers.
